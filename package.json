{
	"name": "bignight-party-2",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"scripts": {
		"dev": "bun run --filter '*' dev",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"test": "bun run --filter '*' test",
		"build": "bun run --filter web build",
		"start": "cd packages/server && bun --env-file=../../.env src/index.ts"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.7",
		"typescript": "^5.9.3"
	}
}
